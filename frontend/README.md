# TraceGraph frontend

Next.js App Router UI for TraceGraph. See the [root README](../README.md) for architecture and setup.

```bash
bun install
bun run dev
```

Set `NEXT_PUBLIC_API_URL` if the API is not at `http://localhost:8000`.
